version: '3.8'

services:
  api: 
    build: ./api
    container_name: ah_petsaddopt_web
    ports:
      - "8021 : 8021"
  
    environment:
      - DB_NAME=Centro_animals
      - DB_USER=base1
      - DB_PASSWORD=123456
      - DB_HOST=db
      - DB_PORT=7012
    networks:
      - ag
networks:
  ag: 
    external : true

  
